import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  bgAlignButton: {
    marginTop: "20px",
    width: "100%",
    display: "flex",
    flexDirection: "row",
    justifyContent: "center",
  },
});
