import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    boxAlertConcluded: {
        position: "relative",
    },
    containerbutton:{
        width: "100%",
        display: "flex",
        justifyContent: "center",
        marginTop: 25,
    }
})