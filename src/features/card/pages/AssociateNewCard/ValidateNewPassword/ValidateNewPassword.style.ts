import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles({
    boxAlertConcluid: {
        position: "relative",
    },
    boxbtn: {
        width: "100%",
        display: "flex",
        justifyContent: "center",
        marginTop: 25
    }
})